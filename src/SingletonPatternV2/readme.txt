This is version V3

This is eager initialisation technique - initialising during
class load time itself, before runtime starts.

Issues ?

You cannot create a custom (by passing arguments during runtime)
instance, as it's already created even before runtime starts.

